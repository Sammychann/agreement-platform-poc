# =============================================================================
# AGREEMENT MANAGEMENT SYSTEM - PYTHON DEPENDENCIES (requirements.txt)
# =============================================================================
# To install all dependencies in VS Code or your terminal:
#     pip install -r requirements.txt
# =============================================================================

# Flask: Simple, lightweight web framework used to run the local web application
flask>=3.1.0

# docxtpl: Python Word template engine (preserves fixed text, clauses, styles, and headers)
docxtpl>=0.20.0

# python-docx: Core library for reading and manipulating Microsoft Word (.docx) files
python-docx>=1.2.0

# docx2pdf: Converts generated Word (.docx) documents to PDF on Windows systems
docx2pdf>=0.1.8

# reportlab: PDF generation engine (used for direct PDF rendering and fallback conversion)
reportlab>=5.0.0

# openpyxl: Excel spreadsheet handling (used for audit logs and Phase 4 folder verification)
openpyxl>=3.1.5

# Pillow: Image processing library
pillow>=12.3.0
